import { AboutPage } from '@/components/page/AboutPage';

export const metadata = {
  title: 'About',
  description: 'Get to know more about Naufal Rafianto',
};

const Page = () => {
  return <AboutPage />;
};
export default Page;
